import React from "react";
import { View, Text, SafeAreaView } from "react-native";

export const EventAddScreen = () => {
  return (
    <SafeAreaView>
      <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
        <Text>{"EventAddScreen"}</Text>
      </View>
    </SafeAreaView>
  );
};
